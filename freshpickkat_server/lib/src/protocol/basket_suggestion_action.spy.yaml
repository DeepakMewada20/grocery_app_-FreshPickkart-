class: BasketSuggestionAction
fields:
  type: String # variant, combo, coupon, bogo, delivery, product
  label: String
  ctaLabel: String
  payload: Map<String, String>?
  productId: String?
  variantId: String?
  comboId: String?
  couponCode: String?
  benefit: double?
  extraSpend: double?
